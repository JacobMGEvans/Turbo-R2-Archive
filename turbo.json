{
	"$schema": "https://turbo.build/schema.json",
	"remoteCache": {
		"signature": true
	},
	"pipeline": {
		"build": {
			"outputs": ["dist/**"],
			"outputMode": "full",
			"inputs": ["src/**"]
		},
		"type:check": {},
		"start": {
			"cache": false,
			"persistent": true
		}
	}
}
